interface:
  display_name: "Vox"
  short_description: "Route Vox CLI workflows for profile, model, ASR, TTS, pipeline, and troubleshooting"
  default_prompt: "Use $vox to route the request to the right Vox CLI workflow for profiles, models, ASR, TTS, pipeline, installation checks, or troubleshooting."
policy:
  allow_implicit_invocation: true
