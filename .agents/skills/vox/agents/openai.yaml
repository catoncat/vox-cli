interface:
  display_name: "Vox"
  short_description: "Natural-language profile, ASR, clone, and pipeline control"
  default_prompt: "Use $vox to manage profiles, add samples, transcribe audio, and synthesize speech with the right Vox CLI workflow."
policy:
  allow_implicit_invocation: true
